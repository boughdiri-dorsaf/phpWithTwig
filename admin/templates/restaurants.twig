
{% extends 'base.twig' %}

{% block title %}Liste des Restaurants{% endblock %}

{% block body %}

	<div class="row">
	    <section class="col-md-12 table-responsive">
	    	<header class="page-header">
		        <h1 class="h3 mb-4 text-gray-800">Liste des Restaurants </h1>
		    </header>
			<div class="row">
				<div class="col-md-12">
					<br \><br \>
					<table class="table table-bordered table-striped table-condensed">
						<thead>
						<tr>
							<th>N°</th>
							<th>Nom</th>
							<th>Image</th>
                            <th>Description</th>
							<th></th>
							<th></th>
						</tr>
						</thead>
						<tbody>
						{% for restaurant in restaurants %}
							<tr>
								<td class="text-black">{{ restaurant.id }}</td>
								<td class="text-black">{{ restaurant.nom }}</td>
								<td class="text-black"><img src="../assets/img/{{ restaurant.image }}" width="250px" height="200px" /> </td>
                                <td class="text-black">{{ restaurant.description }}</td>
								<td class="text-black"><a class="btn btn-success" href="restaurants.php?page=editRestaurant.twig&id={{ restaurant.id }}">Editer</a></td>
								<td class="text-black"><button class="btn btn-danger" data-toggle="modal" data-target="#confirm-delete{{ restaurant.id }}">Supprimer</button></td>
							</tr> 
							<div class="modal fade" id="confirm-delete{{ restaurant.id }}" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
								<div class="modal-dialog">
									<div class="modal-content">
										<div class="modal-header">
											Confirmation de suppression
										</div>
										<div class="modal-body">
											Êtes-vous sûr de vouloir supprimer ce restaurant ?
										</div>
										<div class="modal-footer">
											<button type="button" class="btn btn-default" data-dismiss="modal">Annuler</button>
											<a class="btn btn-danger btn-ok" href="deleteRestaurant.php?id={{ room.id }}">Supprimer</a>
										</div>
									</div>
								</div>
							</div>
						{% endfor %}
						</tbody>
					</table>
				</div>
			</div>
	    </section>
	</div>

	
{% endblock %}