{% extends 'base.twig' %}

{% import 'form.twig' as form %}

{% block title %}Modifier Ce Service{% endblock %}

{% block body %}

<br \>
    <header class="page-header"><meta http-equiv="Content-Type" content="text/html; charset=euc-kr">
        <h1 class="h3 mb-4 text-gray-800">Modifier Ce Service</h1>
    </header>
    <div class="row">
        <div class="col-md-12">
        <br \ ><br \>
            <form id="form1" name="form1" method="post" action="{{ actionEdit }}">
                <div class="row">
                    <div class="col-md-12">
					{% for service in service %}
                        
                        {{ form.inputRequired('f_nom', 'Nom de Service', service.nom) }}

                        {{ form.inputRequired('f_image', 'Image de Service', service.image) }}

                        <div class="row">
                            <div class="col-md-12">
                                {{ form.textareaRequired('f_description', 'Decription', service.description) }}
                            <div>
                        </div>

                        {% endfor %}

                    </div>
                </div>
                <div align="right">
                    <a class="btn btn-secondary" href="services.php?page=services.twig">Annuler</a>
                    <button type="submit" class="btn btn-success">Valider</button>
                </div>
            </form>
        </div>
    </div>
    <br \><br \>
    <script src="https://cdn.ckeditor.com/4.13.0/standard/ckeditor.js"></script>
    <script>
        CKEDITOR.replace( 'f_description' );
    </script>
{% endblock %}